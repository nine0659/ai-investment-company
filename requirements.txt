# AI Investment Research Company - Dependencies

# LangGraph (AI agent pipeline)
langgraph>=0.2.0
langchain-openai>=0.1.0

# OpenAI
openai>=1.0.0

# Scheduler
APScheduler>=3.10.0

# Terminal UI
rich>=13.0.0

# Env vars
python-dotenv>=1.0.0

# HTTP
requests>=2.31.0
httpx>=0.25.0

# Market data (Yahoo Finance)
yfinance>=0.2.0

# News (RSS)
feedparser>=6.0.0

# HTML parsing (증권사 리포트 크롤링)
beautifulsoup4>=4.12.0

# Telegram intelligence (채널 수집)
pyrogram>=2.0.0
# tgcrypto>=1.2.0  # 속도 개선용 (Windows C++ 빌드툴 필요 — 없으면 생략 가능)

# Web App (24/7 대시보드)
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
jinja2>=3.1.0
python-multipart>=0.0.9
aiofiles>=23.2.0

# Database (SQLAlchemy + PostgreSQL/SQLite)
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.0

# DB (SQLite, built-in — no extra package)
# KIS API uses requests

# Korean public holiday calendar (KRX 비거래일 감지용)
holidays>=0.45

# GH Actions 워크플로 YAML 유효성 회귀 테스트용 (tests/test_workflows.py)
PyYAML>=6.0
